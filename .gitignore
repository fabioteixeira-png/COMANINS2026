node_modules
coverage
*.local
.env
.env.*
!.env.example
*.log

# Security/maintenance artifacts must never be committed
*.patch
*.rej
db.json

# Removido bun do projeto (Hostinger requer npm)
bun.lock
